<div class="quote">
  <%= link_to quote.quote, quote_path(quote) %>
  <div class="qoute__actions">
    <%= button_to "Delete", quote_path(quote), method: :delete, calss: "btn btn--light" %>
    <%= link_to "Edit", edit_quote_path(quote), class: "btn btn--light" %>
  </div>
</div>
